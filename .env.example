# Base URL of the teacher-provided movie backend API.
# Left blank until the API contract is provided — see API-CONTRACT.md.
VITE_API_BASE_URL=

# Set to "true" to use local mock movie data for UI development while the
# real backend contract is still TBD. Never set this in production.
VITE_USE_MOCK_API=false
